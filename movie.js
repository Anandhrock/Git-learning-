class movie {

    constructor(title = " ", studion = " ", rating = "PG") {

        this.title = title;
        this.studio = studio;
        this.rating = rating;
    }
    
}
getPG(movies = [movie]){
    var filterarray [ ]
    movie.forEach(movie)=>{
        if (movie.rating = "pg") {
s
            filterarray.push(movie)

            
        }
    }
    return [];
}
console.log(movie)
const spd1 = new movie("spd", "eros");
const avenger = new movie("avenger", "eros");
const spd2 =new movie("spd","eros","pg1")
    
